<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login page</title>
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            background: url('https://seeddownload.cdn-apple.com/s3/prod/ATLC/package/T031485A-en_US/32.0/T031485A-ar-nightsky/images/ar_night_sky_01_poster.jpg') no-repeat center center fixed;
            background-size: cover;
            font-family: Arial, sans-serif;
        }

        .wraper {
            background: rgba(162, 0, 212, 0.9);
            width: 100%;
            max-width: 440px;
            padding: 40px 30px;
            border-radius: 15px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.25);
            color: #ffffff;
            box-sizing: border-box;
        }

        .wraper h1 {
            font-size: 32px;
            margin-bottom: 25px;
            text-align: center;
        }

        .input-box {
            position: relative;
            margin-bottom: 25px;
        }

        .input-box input {
            width: 100%;
            height: 48px;
            padding: 10px 50px 10px 15px;
            font-size: 16px;
            border-radius: 30px;
            border: 2px solid #fff;
            background: transparent;
            color: #ffffff;
            outline: none;
            box-sizing: border-box;
        }

        .input-box input::placeholder {
            color: #eeeeee;
        }

        .input-box i {
            position: absolute;
            right: 15px;
            top: 50%;
            transform: translateY(-50%);
            font-size: 20px;
            color: #ffffff;
        }

        button {
            width: 100%;
            height: 45px;
            border-radius: 30px;
            border: none;
            background-color: #ffffff;
            color: rgb(150, 7, 194);
            font-size: 18px;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.2);
        }

        button:hover {
            background-color: #eeeeee;
            transform: translateY(-2px);
        }

        .remember {
            margin-top: 15px;
            text-align: center;
            font-size: 14px;
        }

        .remember a {
            color: #c0ff03;
            text-decoration: none;
            transition: color 0.2s ease;
        }

        .remember a:hover {
            color: #ffff80;
        }

        .Register {
            color: #04f7e6;
        }
        .text-yellow{
            color: yellow;
        }
        #signupMsg{
            color:#f3f704;
            position: relative;
            top: 230px;
            right:320px;
        }
    </style>
</head>
<body>
    <div class="wraper">
        <form action="/login" method="POST">
            <h1>Login</h1>
               <% if(error) { %>
            <div class="alert alert-danger">
               <%= error %>
            </div>
               <% } %>
            <div class="input-box">
                <input type="email" name="email" placeholder="email id" required>
                <i class='bx bx-user'></i>
            </div>
            <div class="input-box">
                <input type="password" name="password" placeholder="password" required>
                <i class='bx bxs-lock'></i>
            </div>
            <button type="submit">Login</button>
            <div class="mt-3 text-center">
              <a href="/signup" class="text-decoration-none text-yellow">Don't have an account? Signup</a>
            </div>
        </form>
       
    </div>
    <% if (typeof message !== 'undefined' && message) { %>
         <h4 id="signupMsg"><%= message %></h4>
    <% } %>

</body>
</html> 
